#난이도 치고는 은근 생각하기 어려웠던 문제..
target = int(input())
added = 0
num = 0
# 어쨋든 질문은 가장 큰 애를 구하라는 거니까
# 더하다가 숫자가 넘어가면 앞에있는 숫자중 뺄걸 빼면됨,
# 그래서 -1 해줌
# 근데 added < target 하면 added == target일땐 애꿎은 애만 -1되는거니까
# 조건을 "넘었을때만"으로 조정
while added <= target:
    num+=1
    added += num

print(num-1)
